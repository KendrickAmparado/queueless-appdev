{
  "name": "queueless-admin-launcher",
  "version": "1.0.0",
  "private": true,
  "description": "Launcher scripts for running the root app from the Admin folder.",
  "scripts": {
    "start": "npx expo start ..",
    "web": "npx expo start .. --web",
    "android": "npx expo run:android ..",
    "ios": "npx expo run:ios .."
  }
}
